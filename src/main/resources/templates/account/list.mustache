{{>layout/header}}
<!--margin : mt, mr, mb, ml (1~5) ex) mt-5-->
<div class="container mt-2">
    <h1 class="mt-5 mb-4">계좌목록</h1>
    <table class="table table-hover">
        <thead>
        <tr>
            <th>계좌번호</th>
            <th>잔액</th>
        </tr>
        </thead>
        <tbody>

        {{#models}}
        <tr>
            <td><a href="/account/{{number}}">{{number}}</a></td>
            <td>{{balance}}원</td>
        </tr>
        {{/models}}
        </tbody>
    </table>
</div>
<!-- .container end -->
{{>layout/footer}}